import React from "react"
import { ButtonChange, Container } from "./style"

export const Button = () => {
    return (
        <Container>
            <ButtonChange>Click me</ButtonChange>
        </Container>
        
    )
}